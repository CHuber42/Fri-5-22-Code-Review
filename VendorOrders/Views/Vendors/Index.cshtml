@using VendorOrders.Models;

<h1>Vendors Page</h1>

<p>Click <a href="/vendors/new">here</a> to list a new Vendor.</p>
@if (Model.Count > 0)
{
<p>Otherwise, click a vendor below </p>
}

@foreach (Vendor vendor in Model)
{
  <a href="/vendors/@vendor.Id">@vendor.Name</a><br>
}